apiVersion: apps/v1
kind: ReplicaSet
metadata: 
  name: jenkins-rs
  labels:
    app: jenkins
    pupose: demo
spec:
  minReadySeconds: 2
  replicas: 3
  selctors:
    matchLabels:
      app: jenkins
  template:
    metadata:
      labels:
        name: jenkins
    spec:
       containers:
         - name: jenkis
           image: jenkins/jenkins
           ports:
             - containerPort: 8080
               protocol: TCP
    